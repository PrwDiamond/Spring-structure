# ===============================
# = DATA SOURCE
# ===============================
# Set here configurations for the database connection
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: phetchpirat
    password: bqvxssqhjwhvtwxy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/backend
    username: postgres
    password: syspass

app:
  token:
    secret: m#MySesecret2021
    issuer: BackendService
  email:
    from: phetchpirat@gmail.com